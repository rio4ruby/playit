= simple_form_for @album, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :album_dir_id
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), albums_path, :class => 'btn'
