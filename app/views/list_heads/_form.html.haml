= simple_form_for @list_head, :html => { :class => 'form-horizontal' } do |f|
  = f.input :name
  = f.input :is_playing
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), list_heads_path, :class => 'btn'
